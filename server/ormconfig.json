{
    "type": "postgres",
    "host": "localhost",
    "port": 5432,
    "username": "simplex",
    "password": "password",
    "database": "simple_exchange",
    "entities": ["src/**/*.entity{.ts,.js}"],
    "synchronize": false,
    "migrations": ["migrations/*.ts"],
    "cache": {
      "type": "ioredis",
      "options": {
        "host": "localhost",
        "port": 6379
      }
    },
    "cli": {
      "migrationsDir": "migrations"
    }
}
